#include <stdio.h>
 
int main() {
 
    int number, hours;
    double salary_hour, salary;
    scanf("%d",&number);
    scanf("%d",&hours);
    scanf("%lf",&salary_hour);
    salary = salary_hour * (double)hours;
    printf("NUMBER = %d\n", number);
    printf("SALARY = U$ %.2lf\n", salary);

}
